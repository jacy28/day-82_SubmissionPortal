services:
  - type: web
    name: resume_submission
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "python manage.py migrate && gunicorn resume_submission.wsgi"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
