services:
  - type: web
    name: resume_submission
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn resume_submission.wsgi"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
